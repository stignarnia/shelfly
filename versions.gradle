ext.versions = [
    versionCode: 4,
    versionName: '4.0.3',

    minSdk     : 23,
    compileSdk : 37,
    targetSdk  : 36,

    buildTools : '37.0.0',
    // Bytecode target. Stays at 21 (Android's ceiling) regardless of the JDK
    // running Gradle - see the toolchain block in the root build.gradle.
    jvmTarget  : 21
]
